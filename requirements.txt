# BossMind dependencies (Python 3.12)
#
# 推荐安装方式（与 pyproject.toml 对齐）：
#
#   conda create -n BossMind python=3.12
#   conda activate BossMind
#
#   # 可编辑安装 + 按设备选 extras
#   pip install -e ".[collect,dev]"          # B 机：采数 / probe / 钩子
#   pip install -e ".[data,train,dev]"        # A 机 / WSL：数据集 + 训练辅助
#   pip install -e ".[collect,data,train,dev,perception,inference,llm]"  # 全量
#
# 若习惯 requirements 文件，也可：
#   pip install -r requirements.txt
#   pip install -e .
#
# PyTorch（仅 WSL2 + ROCm，RX 7900 XT；勿在 Windows 装 CUDA 版）：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm7.2
#   （版本以 AMD Radeon WSL 文档为准）

# ---------------------------------------------------------------------------
# 核心（对应 pyproject [project].dependencies）
# ---------------------------------------------------------------------------
numpy>=1.26,<3
pydantic>=2.6,<3
PyYAML>=6.0.1,<7
pymem>=1.13,<2

# ---------------------------------------------------------------------------
# collect：真环境按键（B 机）
# ---------------------------------------------------------------------------
pydirectinput>=1.0.4,<2
pynput>=1.8.2,<2

# ---------------------------------------------------------------------------
# perception：截屏 / ROI（Phase 2+ CNN）
# ---------------------------------------------------------------------------
mss>=9.0.1,<11
Pillow>=10.2,<12
opencv-python-headless>=4.9,<5

# ---------------------------------------------------------------------------
# data：JSONL / Parquet
# ---------------------------------------------------------------------------
pandas>=2.2,<3
pyarrow>=15.0,<20

# ---------------------------------------------------------------------------
# inference：局内推理 / 模型导出
# ---------------------------------------------------------------------------
onnx>=1.16,<2
onnxruntime>=1.17,<2

# ---------------------------------------------------------------------------
# train：WSL 训 BC/CNN（PyTorch 见文件头注释，单独安装）
# ---------------------------------------------------------------------------
scikit-learn>=1.4,<2
tensorboard>=2.16,<3
tqdm>=4.66,<5
# wandb>=0.16,<1
# gymnasium>=0.29,<1
# stable-baselines3>=2.3,<3

# ---------------------------------------------------------------------------
# llm：策略家 / 训练师（Phase 3+）
# ---------------------------------------------------------------------------
openai>=1.40,<2
httpx>=0.27,<1
tenacity>=8.2,<10
# chromadb>=0.5,<1
# sentence-transformers>=3.0,<4

# ---------------------------------------------------------------------------
# dev
# ---------------------------------------------------------------------------
pytest>=8.0,<9
pytest-cov>=5.0,<7
ruff>=0.6,<1
# mypy>=1.10,<2
# types-PyYAML>=6.0
