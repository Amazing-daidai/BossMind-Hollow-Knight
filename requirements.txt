# BossMind dependencies (Python 3.12)
#
#   conda create -n BossMind python=3.12
#   pip install -r requirements.txt
#
# PyTorch (WSL2 + ROCm only, RX 7900 XT):
#   Install from AMD index AFTER this file, e.g.:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm7.2
#   (Confirm version on AMD Radeon WSL docs; do NOT install CUDA torch on Windows.)
#
# Optional lines are commented; uncomment when needed.

# ---------------------------------------------------------------------------
# 核心：配置 / 数据模型 / 数值
# ---------------------------------------------------------------------------
numpy>=1.26,<3
pydantic>=2.6,<3
PyYAML>=6.0.1,<7

# ---------------------------------------------------------------------------
# 真环境：内存读（Windows） / 按键
# ---------------------------------------------------------------------------
pymem>=1.13,<2
pydirectinput>=1.0.4,<2
# vgamepad>=0.1.0

# ---------------------------------------------------------------------------
# 感知：截屏 / ROI（Phase 2+ CNN）
# ---------------------------------------------------------------------------
mss>=9.0.1,<11
Pillow>=10.2,<12
opencv-python-headless>=4.9,<5

# ---------------------------------------------------------------------------
# 采集与落盘：JSONL / Parquet
# ---------------------------------------------------------------------------
pandas>=2.2,<3
pyarrow>=15.0,<20
pynput>=1.8.2,<2

# ---------------------------------------------------------------------------
# 局内推理 / 模型导出
# ---------------------------------------------------------------------------
onnx>=1.16,<2
onnxruntime>=1.17,<2

# ---------------------------------------------------------------------------
# 训练辅助（WSL 训 BC/CNN；与 Windows 共用同一份 requirements）
# ---------------------------------------------------------------------------
scikit-learn>=1.4,<2
tensorboard>=2.16,<3
tqdm>=4.66,<5
# wandb>=0.16,<1
# gymnasium>=0.29,<1
# stable-baselines3>=2.3,<3

# ---------------------------------------------------------------------------
# LLM Agent：策略家 / 训练师
# ---------------------------------------------------------------------------
openai>=1.40,<2
httpx>=0.27,<1
tenacity>=8.2,<10
# chromadb>=0.5,<1
# sentence-transformers>=3.0,<4

# ---------------------------------------------------------------------------
# 开发工具（可选）
# ---------------------------------------------------------------------------
pytest>=8.0,<9
pytest-cov>=5.0,<7
ruff>=0.6,<1
# mypy>=1.10,<2
# types-PyYAML>=6.0
